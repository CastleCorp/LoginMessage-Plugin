package com.gmail.pthomas1997.loginMessagePlugin;

import org.bukkit.command.Command;
import org.bukkit.command.CommandExecutor;
import org.bukkit.command.CommandSender;

public class WelcomeCommmand implements CommandExecutor{

	/**
	 * onCommand(CommandSender sender, Command cmd, String label, String[] args)
	 * When executed, will return the "Welcome Message" as defined in the config.yml
	 */
	public boolean onCommand(CommandSender sender, Command cmd, String label, String[] args){
		if(cmd.getName().equalsIgnoreCase("welcome")){ 
			if(sender.hasPermission("loginmessage.welcome"));
			sender.sendMessage(LoginMessagePlugin.getWelcomeMessage());
			return true;
		} 
		return false; 
	}
}
